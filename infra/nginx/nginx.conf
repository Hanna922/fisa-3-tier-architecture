user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice; # 에러 로그 저장 위치
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;
    sendfile        on;
    keepalive_timeout  65; 

    upstream backend-servers {
        # Docker 내부에서 외부로 요청을 보내기 때문에 'host.docker.internal' 키워드를 사용.
        server host.docker.internal:8080;
        server host.docker.internal:8090;
    }

    server {
        # 첫 번째 server 블록
        # localhost:80으로 들어오는 요청을 처리할 서비스할 서버를 정의한다. 
        listen 80; # for IPV4 

        server_name localhost;  # localhost 도메인으로 들어온 요청에 대해 처리

        location / {
            proxy_pass http://backend-servers;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
